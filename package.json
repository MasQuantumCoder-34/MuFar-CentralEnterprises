{
  "name": "mufar-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "Mufar Commerce - Enterprise Distribution & Order Management Platform",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:admin": "cd admin-panel && npm run dev",
    "dev:mobile": "cd mobile-app && npm start",
    "build:shared": "cd shared && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:admin": "cd admin-panel && npm run build",
    "install:all": "npm install && cd shared && npm install && cd ../backend && npm install && cd ../admin-panel && npm install && cd ../mobile-app && npm install",
    "lint": "cd backend && npm run lint && cd ../admin-panel && npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mufar-technologies/mufar-commerce"
  },
  "author": "Mufar Technologies",
  "license": "UNLICENSED"
}
